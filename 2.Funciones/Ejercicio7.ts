/*usando typescript Crea una función llamada calcularAreaTriangulo que tome la base y la altura de un triángulo como parámetros y devuelva su área*/

const calcularAreaTriangulo = (base:number, altura:number)=>(base * altura) / 2;

console.log(calcularAreaTriangulo(8, 10));